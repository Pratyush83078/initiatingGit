INSTALLED :
	I3 INSTALLATION i3, startx(x11,xorg) light , redshift , $mod Mod4 , 
	a file manager pcmanfm, terminal file manager 
	(rover,nnn),aslatter/ppa (alacritty),ungoogled chromium best, feh, 
	pip3 install autotiling (python3,python3-pip),python3 -m site 
	--user-base,pip3 list | grep autotiling , pavucontrol ,zsh (Oh my 
	zsh)


QUES:
	generally i download packages by sudo apt install etc but after that i forget about it ..
	what is --no-startup-id:

COMMANDS
	bindsym XF86AudioRaiseVolume exec --no-startup-id pactl 
	set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
	nmcli dev wifi connect "iQOO Z7 5G" password "deadpool", 
	bluetoothctl.
	--no-install-recommends
	--no-install-recommends
	

GIT:
	
	git config --global credential.helper store
	git config --global credential.helper cache
	to remove it no worry just use:
		git config --global --unset credential.helper
		git credential-cache exit  && can also remove the file rm ~/.git-credentials
		
		

USEFUL WEBSITE
	http://htmlpreview.github.io/?
